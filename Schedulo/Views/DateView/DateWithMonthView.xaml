<UserControl x:Class="Schedulo.Views.DateView.DateWithMonthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Schedulo.Views.DateView"
             mc:Ignorable="d" 
             d:DesignHeight="640" d:DesignWidth="1008">

    <Border CornerRadius="4" BorderThickness="1" BorderBrush="{StaticResource GrayColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <UniformGrid Columns="7">
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="MON" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="MON" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="TUE" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="WED" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="THU" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="FRI" HorizontalAlignment="Center"/>
                </Border>
                
                <Border BorderBrush="{StaticResource GrayColor}" BorderThickness="1px">
                    <TextBlock Text="SAT" HorizontalAlignment="Center"/>
                </Border>
            </UniformGrid>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Days}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7" Rows="6"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource BorderCellDay}">
                            <StackPanel>
                                <TextBlock 
                                    Text="{Binding Date, StringFormat='{}{0:dd}'}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Top"
                                    FontWeight="Bold"/>

                                <ItemsControl ItemsSource="{Binding Events}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="LightBlue" CornerRadius="5" Padding="2" Margin="2">
                                                <TextBlock Text="{Binding Name}" FontSize="12"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
