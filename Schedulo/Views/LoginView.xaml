<Window x:Class="Schedulo.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Schedulo.Views"
        xmlns:resources="clr-namespace:Schedulo.Properties"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="SignUpView" Height="540" Width="720">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320px"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="200px"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Margin="0 64 0 0" Text="{x:Static resources:Resources.S01}" Style="{StaticResource HeaderTextStyle}"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
